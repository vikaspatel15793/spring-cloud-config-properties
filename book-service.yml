spring:
  application:
    name: book-service

server:
  port: 8083


eureka:
  client:
    serviceUrl:
      defaultZone: http://localhost:8082/eureka
  instance:
    preferIpAddress: true


  
#management:
#  endpoints:
#    web:
#      exposure:
#        include: hystrix.stream
